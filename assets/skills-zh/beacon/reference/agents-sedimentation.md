# AGENTS 沉淀判断

- 不适合长期复用的一次性背景、临时 workaround 或单次 feature 说明，应静默忽略，不写入 AGENTS。
- 只有当归档内容属于长期有效、对 AI 高价值的规则时，才先给出摘要级沉淀建议，再等待用户确认。
- 项目尚未建立 AGENTS 体系时，按“全量维护 + 当前归档注入”调用 `/beacon-init`。
- 项目已经存在 AGENTS 体系时，只做与当前归档相关的增量维护，不借机重写整棵树。
- 无论是手动维护还是 archive 转调，创建、更新、合并、迁移和删除都先给摘要建议，再执行落盘。
