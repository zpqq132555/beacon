# AGENTS 拓扑

- 根 `AGENTS.md` 只保留全局入口、关键命令、高价值注意事项和 AGENTS Map。
- 当目录边界与职责边界一致时，优先使用该目录下的 `AGENTS.md` 作为局部入口。
- 当目录本身承载多种长期稳定职责时，使用 `本地 AGENTS.md + [职责].md` 组织局部规则。
- 混合目录自身的职责文档与其子目录下的 `AGENTS.md` 可以并存，不必二选一。
- 只有在职责继续细分且长期稳定时，才继续向更深层级创建新的 AGENTS 节点。
